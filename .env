MAX_CONTENT_LENGTH=1048576
MAX_PENDING_TIME=600
MAX_STORE_TIME=259200
FLASK_DEBUG=0
FLASK_ENV=production
DB_URI=postgresql://aperiuser:aperipass@postgres:5432/aperisolve

# Redis is optional. If REDIS_URL is set, tasks will run asynchronously via Redis Queue.
# If not set, tasks will run synchronously (suitable for free-tier deployments like Render).
# Uncomment the line below to enable Redis (for Docker Compose with local Redis):
# REDIS_URL=redis://redis:6379/0

RQ_DASHBOARD_REDIS_URL=redis://redis:6379/0
POSTGRES_DB=aperisolve
POSTGRES_USER=aperiuser
POSTGRES_PASSWORD=aperipass